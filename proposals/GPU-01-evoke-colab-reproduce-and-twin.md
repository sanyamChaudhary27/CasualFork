# GPU Experiment Proposal — GPU-01

**Status: PATCH_INCOMPLETE — NOT EXECUTED. Final launch-attestation review returned two PATCH_INCOMPLETE verdicts concerning stale prelaunch-archive reuse/freshness. GPU authorization remains absent.**

## Required fields (per COMPANY.md)

- **Research question:** Can we reproduce one official EVOKE generation and then produce one short controlled twin continuation using the validated fork mechanism? (G1 + the first strict-coupled branch proof, nothing more.)
- **Hypothesis (v2, synced to shipped patch + validator-v2 semantics):** With the strict-coupling patch applied to pinned tree `74d268516d95c8fceadd2378f91a73f9f187042b` — top-of-chunk-loop fork hook (capture/restore of main + `_geo_patchdrop_gen` generators + FORK_STATE_DIGEST sidecars), env-gated ledger over R1–R7 with per-site comparison modes (R1/R4/R5/R6 exact tensor; R2/R3 stream-witness + mean/std/input diagnostics, byte-equal at fork chunk; R7 isolated-stream chain with explained-divergence rule), grammar-enforced draw order (R7→R2→R3→R4→R5→R1→R6×2), and `compare_coupling_logs` v2 with pair-manifest binding + FORK_STATE_DIGEST precondition — twin branches from one forked prefix under byte-identical configs (prompt-schedule-only divergence) validate STRICT_NOISE_COUPLED (mode A), while a fresh-RNG control returns STRICT_COUPLING_INVALID/TENSOR_MISMATCH (mode B).
- **Decision affected:** Whether SC1 proceeds on EVOKE as backend (SPEC §2a strict-coupled achievable) or downgrades per the fallback rule; whether G1→G5 gates advance.
- **Cheapest falsification:** O1 desync positive control (must return INVALID) and O4 instance-leak control (any unexplained difference blocks SC1). Protocols frozen in experiments/preregistrations/O1-O4-falsification-rides.md.
- **CPU alternative (final launch-attestation correction; review pending):** model-free 8/8; compatibility 12/12; real-EVOKE 9/9; v2 semantics 76/76. Patch apply-check CLEAN and fresh applied tree byte-identical (+1538/−1; sha256=`ee25e02c479c21aa214e630f8165fd67b6d7bc10f1f0c30ac4b06786fd920970`). One canonical config implementation is copied byte-identically into EVOKE and used by preflight, wrapper, and engine. Mandatory `gpu01_launch.py` archives PASS before exact-list child execution; engine independently recomputes after argparse and requires the bound archive; validator binds both branch archives to manifest and ledgers. L1–L12 pass. At G1 still required: exact runtime package identity, successful flash-attn probe, measured hardware/VRAM, and same-seed CUDA deltas.
- **Compute policy:** Colab = G0 hardware report + all static/harness checks ONLY. **If measured VRAM < proven model-load floor → `G0_GPU_CAPACITY_INSUFFICIENT`, no model load attempted.** An A100-class probe may be proposed but is never represented as guaranteed or as the faithful recipe.
- **Proven memory floors (source-backed @ pin):** ~28 GB resident/pipeline (infer_single.py:1097); 50.4 GB measured peak/worker at shipped recipe (infer_post_distill.sh); host build 54 GB transformer + 22 GB text encoder; CUDA unconditional (:545–546, :775); no upstream inference CPU-offload (grep-verified 2026-08-25).
- **Requested GPU / VRAM:** **Lightning/H100-class ≥80 GB** for the faithful run at shipped recipe (384×640, STAGE2_STEPS="1 1 1", CFG off, VAE_DECODE_TYPE=persistent, DEPTH_BACKEND=vigeo). T4/L4 removed (< resident floor). A100-40GB: only the non-guaranteed one-chunk probe if offered. No quantization/offload engineering (scope creep absent a trivial upstream-supported mode).
- **Expected paid-GPU consumption:** ≤2 h on the ≥80 GB runtime: 0.5 h environment bring-up (dev-fork diffusers risk), 0.5 h G1 official reproduction, 1 h for the frozen first research run below including mandatory O1/O4 rides before any SC1 number counts.
- **Frozen first research run (minimal coupling proof):** exactly ONE prefix chunk generated from the official sample path, fork at the chunk-1 boundary, then EXACTLY ONE post-fork chunk per branch: factual continuation vs strict-coupled intervention branch (mode A), plus uncoupled control (mode B) and the two falsification rides. Multi-post-fork-chunk rollouts are a LATER extension gated on this passing.
- **Archival launch-strict contract:** the pair manifest MUST contain the literal JSON field `"launch_strict": true`; missing, false, null, string `"true"`, and integer `1` are refused. The runtime also exports `EVOKE_STRICT_LAUNCH=1`. Run `harness/gpu01_prelaunch.py` before importing EVOKE or constructing a model; it validates proposal/pin/patch/profile/config identity, seeds, fresh IDs, literal strictness, flash-attn, and writes the environment fingerprint before any hypothetical model load. It and the patched strict process set/record `torch.backends.cudnn.benchmark=False` and `.deterministic=True`; this is not a claim of complete CUDA bitwise determinism. Before any SC1 result is admissible, archive `refuse_gpu_countersign(validation_result) == False` with that manifest.
- **Success criterion (predeclared, v2):** (a) G1 official sample visually sane at conservative settings AND same-seed rerun delta recorded; (b) FORK_STATE_DIGEST equality between branches at the boundary (fail-loud otherwise); (c) prefix artifact hashes byte-identical; (d) mode-A pair validates STRICT_NOISE_COUPLED under validator-v2 — events verified at fork chunk, meta fully compared (pin/patch/profile/torch/diffusers/warp_seed/config/fork_chunk), grammar order satisfied, all 7 sites covered with per-site adjudicated comparison modes; (e) mode-B returns INVALID via failure of an adjudicated site-specific exogenous-randomness witness; (f) O1/O4 behave as pre-registered; (g) gates-unset byte-neutrality check recorded on the GPU box; (h) pip freeze archived.
- **Mode A success:** all adjudicated site-specific exogenous-randomness witnesses pass.
- **Failure criterion:** any of — official sample unreproducible after environment verification; child FORK_STATE_DIGEST mismatch; failure of an adjudicated site-specific exogenous-randomness witness under restored state + byte-identical configs (note: R2/R3 realized latents and R7 realized indices are NOT universally required to match — see adjudication table); unexplained O4 difference; O1 false PASS → instrumentation invalid, SC1 stops, GF0 reopens; otherwise downgrade to prefix-shared-seed-matched per SPEC §2a.
- **Artifacts to preserve:** G0 report incl. measured pipeline VRAM residency; exact commands + logs + pip freeze; pin commit + patch file + patch sha256; per-site ledgers both branches; pair manifest + FORK_STATE_DIGEST JSONs; decoded-latent hashes; harness-format manifests; clips/latents under ignored artifacts dir with hashes in the EXPERIMENTS.md entry.
- **Reviewer countersignature:** NOT GRANTED — M `ses_faeee99b1ffew615IOHAcsmDqM` = PATCH_INCOMPLETE; G `ses_faeee991dffe9pwqB7ukJBNqmo` = PATCH_INCOMPLETE. No GPU session may start.

## Constraints honored
No search, no RL, no product frontend, no Blender benchmark, no hero-seed hunting. One official generation, one prefix chunk, one controlled twin continuation per branch. License note: both depth backends CC-BY-NC-4.0 — research-use only framing.
