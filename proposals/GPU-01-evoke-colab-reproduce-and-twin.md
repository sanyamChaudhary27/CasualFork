# GPU Experiment Proposal — GPU-01

**Status: DRAFT — NOT EXECUTED. Requires reviewer countersignature before launch (COMPANY.md GPU experiment proposal gate).**

## Required fields (per COMPANY.md)

- **Research question:** Can we reproduce one official EVOKE generation and then produce one short controlled twin continuation using the validated fork mechanism? (Colab-first; this is G1 + the first strict-coupled branch proof, nothing more.)
- **Hypothesis (synced to shipped patch, post-review):** With the strict-coupling patch applied to pinned tree `74d268516d95c8fceadd2378f91a73f9f187042b` — env-gated stochastic ledger over the 7 reachable post-fork sites (R1–R7), fork capture/restore of main + `_geo_patchdrop_gen` generators at the chunk-boundary hook, and validator `compare_coupling_logs` — twin branches from one forked prefix under byte-identical configs (prompt-schedule-only divergence, empty identical event_chunks) will show post-fork per-site noise-tensor hash equality (mode A), while a fresh-RNG control branch shows inequality (mode B). Recipe: STAGE2 pyramid "1 1 1" (R6 renoise; no DMD path), CFG off, persistent decode.
- **Decision affected:** Whether SC1 proceeds on EVOKE as backend (SPEC §2a strict-coupled achievable) or downgrades per the fallback rule; whether G1→G5 gates advance.
- **Cheapest falsification:** The O1 dual-run desync demonstration — same-seed runs differing only by an event chunk must produce unequal post-fork noise hashes; if instead our instrumentation cannot detect this, the coupling methodology is invalid. Second: shared-vs-fresh-instance comparison (O4); if outputs differ, state isolation fails and no branch claim is possible. Full protocols frozen in experiments/preregistrations/O1-O4-falsification-rides.md.
- **CPU alternative (done):** model-free harness 8/8 PASS; strict-ledger suite 11/11; real-class EVOKE state tests 9/9 (direct import from pinned tree, torch 2.10.0+cpu); stochastic-site inventory complete (7 reachable / 7 instrumented / 10 disabled / 0 unknown). Remaining pre-launch CPU work: none blocking; patch apply-check already CLEAN.
- **Colab role (revised policy):** Colab is the G0 + static-validation environment ONLY: run G0 hardware report, all harness/patch/ledger checks, and the O1/O4 protocol dry validation against mock logs. **If measured VRAM is below the proven model-load floor, return `G0_GPU_CAPACITY_INSUFFICIENT` and DO NOT attempt model load.** If an A100-class (40–80 GB) runtime happens to be available, a reduced ONE-CHUNK memory probe may be proposed as explicitly non-guaranteed exploratory work — never represented as the faithful recipe.
- **Proven memory floors (source-backed @ pin 74d26851):** one resident EvokePipeline ≈ **28 GB VRAM** (`_PIPE_CACHE` comment, infer_single.py:1097 — "a second key would double 28 GB of VRAM"); shipped post-distill recipe = **50.4 GB measured peak per worker process** (infer_post_distill.sh: "2 fit in an H200's 139.8 GB and 3 do not"); host-RAM build peak 54 GB transformer + 22 GB text encoder per pipeline build. Pipeline requires CUDA unconditionally (infer_single.py:545–546, :775). **No inference-path CPU-offload mode exists upstream** (grep-verified 2026-08-25: only training-time gradient-checkpointing/expert-offload utilities present).
- **Requested GPU / VRAM:** primary target **Lightning/H100-class ≥80 GB VRAM** for the faithful G1 + twin continuation at the shipped recipe (384×640, STAGE2_STEPS="1 1 1", CFG off, VAE_DECODE_TYPE=persistent, DEPTH_BACKEND=vigeo). T4/L4 (16–24 GB) is BELOW the proven ~28 GB resident floor and cannot load one pipeline faithfully; it is removed as an assumed target. A100-40GB sits between resident and peak floors: usable only for the explicitly-non-guaranteed one-chunk probe if offered.
- **Expected paid-GPU consumption:** ≤2 h on the ≥80 GB runtime: 0.5 h environment bring-up (dev-fork diffusers install is the known risk), 0.5 h official-sample reproduction (G1), 1 h twin-continuation runs (factual + mode-A strict-coupled + mode-B uncoupled control) including the mandatory O1/O4 rides before any SC1 number counts.
- **Success criterion (predeclared, post-review wording):** (a) unmodified official sample produces visually sane output (G1); (b) prefix identity proven by stored prefix-artifact hashes (byte-identical), then STRICT_NOISE_COUPLED from `compare_coupling_logs` on the two branches' post-fork ledgers — all 7 sites present, ordinals aligned, tensor_sha256 byte-equal, no bypass indicators; (c) mode-B control returns STRICT_COUPLING_INVALID with TENSOR_MISMATCH; (d) O1/O4 rides behave as pre-registered. Pre-launch conditions from the sealed review (2026-08-25) must be closed first: validator v2 (distinct-file guard, meta comparison incl. pin/torch/warp_seed/restore events, lazy-meta fix), fork-hook relocation to true chunk boundary (top-of-chunk-loop) or documented resume semantics with warp-draw parity assertion, inventory backfill (v2v-block rows UNREACHABLE-i2v; randperm row; extended battery patterns randperm|np.random|random.).
- **Failure criterion:** any of — official sample unreproducible after environment verification; post-fork noise hashes unequal despite restored generator state and identical configs; draw-log desync detected without a declared cause → downgrade that path to prefix-shared-seed-matched and trigger SPEC §2a fallback rule.
- **Artifacts to preserve:** environment report (G0 fields); exact commands + logs; pinned commit + our patch diff; per-chunk per-draw noise-tensor hashes for every branch; decoded-latent hashes via EVOKE_SAVE_DECODE_LATENTS; branch manifests in harness manifest format; all clips/latents under ignored artifacts dir with hashes recorded in EXPERIMENTS.md entry.
- **Reviewer countersignature:** PENDING — dispatch independent reviewer on this proposal + patch diff before any Colab GPU session starts.

## Constraints honored
No search, no RL, no product frontend, no Blender benchmark, no hero-seed hunting. One official generation, one controlled twin continuation. License note: both depth backends CC-BY-NC-4.0 — research-use only framing.
